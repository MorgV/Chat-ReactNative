{
  "compilerOptions": {
    // Лэйаут
    "rootDir": "./src",
    "outDir": "./dist",

    // Среда
    "module": "NodeNext",         // для поддержки ESM в Node.js
    "moduleResolution": "NodeNext",
    "target": "ES2020",           // можно ESNext, но ES2020 стабильнее для Node.js
    "lib": ["ES2020"],
    "types": ["node"],

    // Карта исходников
    "sourceMap": true,

    // Типизация
    "strict": true,
    "skipLibCheck": true,

    // Для ESM
    "allowImportingTsExtensions": true,  // если хочешь писать `import ... .ts`
    "esModuleInterop": true,
    "resolveJsonModule": true,

    // Не мешать работать с импортами
    "verbatimModuleSyntax": false,
    "isolatedModules": false
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
